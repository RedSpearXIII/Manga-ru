.inputContainer {
  label {
    @apply text-sm;
  }
  div {
    @apply bg-slate-200 dark:bg-slate-800 rounded-lg flex items-center pr-5 drop-shadow;
    input {
      @apply p-2 w-full;
    }
    svg {
      @apply ml-3 text-2xl;
    }
  }
}
