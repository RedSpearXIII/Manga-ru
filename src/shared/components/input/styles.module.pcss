.inputContainer {
  label {
    @apply text-sm;
  }
  div {
    @apply bg-gray-200 dark:bg-gray-800 rounded-lg flex items-center pr-5;
    input {
      @apply py-2 w-full;
    }
    svg {
      @apply mx-4 text-2xl;
    }
  }
}
