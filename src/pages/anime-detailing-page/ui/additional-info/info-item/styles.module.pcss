.item {
  .title {
    @apply text-sm text-center;
  }

  .value {
    @apply bg-[var(--card-background-color)] rounded text-center py-0.5;
    p {
      @apply text-[var(--card-background-text-color)] text-sm;
    }
  }
}
